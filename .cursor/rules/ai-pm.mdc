# AI Product Manager - Cursor Rules

This project uses the AI Product Manager knowledge kit. The rules are defined in `AGENTS.md` at the project root.

For skill definitions, see `skills/` directory.
For reference documents, see `references/` directory.
For templates, see `templates/` directory.